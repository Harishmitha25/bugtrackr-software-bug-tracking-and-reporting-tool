# Shared secret sent as the x-similarity-key header when calling the backend
SIMILARITY_API_KEY=

# Deployed backend URL (used to fetch existing bugs on startup to rebuild the FAISS index)
BACKEND_URL=https://localhost:5000
